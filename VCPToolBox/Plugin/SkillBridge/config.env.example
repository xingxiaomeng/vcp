# SkillBridge 顶层提示词，会作为 0.0 阈值区块的内容输出
SKILLBRIDGE_HEADER_TEXT=这里是Skill技能目录，若你需要对应技能，请使用文件管理插件的Ink模式读取技能。

# 所有 Skill 索引区块默认使用的全局阈值
SKILLBRIDGE_DEFAULT_THRESHOLD=0.35

# 路径输出模式：
# absolute_windows = Windows 真实绝对路径（默认）
# relative = 相对 Plugin/SkillBridge/ 的路径
SKILLBRIDGE_PATH_MODE=absolute_windows